const pry = require('pryjs')

const timesTable = (rows) => {
  let count = 1
  let table = ''
  // for(let i = count; i < rows + 1; i++) {
  //   table += `${i} `
  //   for(let j = count; j < rows + 1; i++) {
  //
  //   }
  //
  // }
  //eval(pry.it)
  return table
}

module.exports = timesTable
