const assert = require('chai').assert
const timesTable = require('../timesTable')

describe('the total function', function() {
	xit('can return the longest string in an array', function() {
		assert.equal(timesTable(5), "");	
	})
});

